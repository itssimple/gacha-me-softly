namespace Chris.PachiRogue.Run
{
    // Run state machine, stages, drafting, meta — implemented in Phase 4
    // (PLAN.md). This marker keeps the assembly non-empty so the asmdef
    // compiles cleanly.
    internal static class AssemblyMarker
    {
    }
}
