namespace Chris.PachiRogue.ProcGen
{
    // Chunk library, stage assembler, validation — implemented in Phase 3
    // (PLAN.md). This marker keeps the assembly non-empty so the asmdef
    // compiles cleanly.
    internal static class AssemblyMarker
    {
    }
}
