namespace Chris.PachiRogue.UI
{
    // HUD, draft screen, menus — implemented in Phase 4 (PLAN.md). All
    // player-facing strings go through Unity Localization tables (en, sv).
    // This marker keeps the assembly non-empty so the asmdef compiles cleanly.
    internal static class AssemblyMarker
    {
    }
}
